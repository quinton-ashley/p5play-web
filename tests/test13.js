function setup() {
	createCanvas(500, 200);

	//               (x, y, [length, angle, repeat])
	let s = new Sprite([
		[10, 10],
		[100, 180],
		[490, 100],
		[10, 10]
	]);
}

function draw() {
	clear();
}
